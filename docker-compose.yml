version: '3.8'

services:
  app:
    build: .
    ports:
      - "8080:80"
    volumes:
      - ./tempmail.yml:/var/www/html/tempmail.yml
      - ./data:/var/www/html/data
    restart: unless-stopped
